name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  gauntlet:
    runs-on: ubuntu-latest
    container: texlive/texlive:latest
    steps:
      - uses: actions/checkout@v4
      - name: Install test dependencies
        run: |
          apt-get update
          apt-get install -y --no-install-recommends rsync procps python3 python3-pip shellcheck
          pip3 install --break-system-packages pypdf pygments
      - name: Lint shell scripts
        run: shellcheck -s sh scripts/safe-build.sh tests/run_tests.sh
      - name: Run the test gauntlet
        run: sh tests/run_tests.sh
